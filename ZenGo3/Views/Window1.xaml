<Window
    x:Class="ZenGo3.Window1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ZenGo3"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ZenGo3.ViewModels"
    Title="ZenGo3"
    MinWidth="800"
    MinHeight="600"
    MaxWidth="800"
    MaxHeight="600"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">

    <d:Window.DataContext>
        <vm:Window1VM />
    </d:Window.DataContext>
    <Grid Background="#EDE8D0">
        <!--<StackPanel Background="Yellow" Opacity="0.8">
            <TextBlock Text="{Binding GameStats.ActivePlayer, StringFormat='Active player : {0}'}" FontWeight="Bold"/>
        </StackPanel>-->
        <!--  Debug Panel - Remove when done  -->
        <!--
        <StackPanel Background="Yellow" Opacity="0.8">
            <TextBlock Text="{Binding GameStats.Grid.Length, StringFormat='Total Cells: {0}'}" FontWeight="Bold"/>
            <TextBlock Text="{Binding GameStats.BlackCaptures, StringFormat='Black Captures: {0}'}" FontWeight="Bold"/>
            <TextBlock Text="{Binding GameStats.WhiteCaptures, StringFormat='White Captures: {0}'}" FontWeight="Bold"/>
            <TextBlock Text="{Binding GameStats.Handicap, StringFormat='Handicap: {0}'}"/>
            <TextBlock Text="{Binding GameStats.ActivePlayer, StringFormat='Current Player: {0}'}"/>
            <TextBlock Text="{Binding GameStats.CurrentTime, StringFormat='Current Player: {0}'}"/>
            <TextBlock Text="{Binding GameStats.Komi, StringFormat='Komi: {0}'}"/>
            <TextBlock Text="{Binding GameStats.BlackSurnom, StringFormat='black surnom: {0}'}"/>
            <TextBlock Text="{Binding GameStats.WhiteSurnom, StringFormat='white surnom: {0}'}"/>
            <TextBlock Text="{Binding GameStats.BlackPlayTimeSeconds, StringFormat='blakc play time: {0}'}"/>
        </StackPanel>-->
        <local:SettingsUC x:Name="SettingsUC" Loaded="SettingsUC_Loaded" />
        <local:GameView
            x:Name="GameView"
            IsEnabled="False"
            Visibility="Collapsed" />
        <local:EndGameUC x:Name="EndGameUC" Visibility="Collapsed" />


    </Grid>
</Window>
